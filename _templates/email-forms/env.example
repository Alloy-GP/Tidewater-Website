# ── Resend ─────────────────────────────────────────────────────────
# Get from resend.com → API Keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# ── Google Sheet (newsletter subscribers) ───────────────────────────
# Where /api/subscribe and the contact form's newsletter opt-in write.
# Set up by scripts/google-apps-script/newsletter-sheet.gs — that file has the
# full walkthrough. URL is the Apps Script web-app deployment (ends in /exec);
# TOKEN must match the script's TOKEN script property. Generate it with:
#   openssl rand -hex 32
SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/xxxxxxxxxxxx/exec
SHEETS_WEBHOOK_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Mailchimp (proposal-intake leads only — /api/lead) ──────────────
# API key: Mailchimp → Account → Extras → API Keys
# Server prefix: the letters/numbers before .api.mailchimp.com in your API URL (e.g. "us21")
# Audience ID: Mailchimp → Audience → Settings → Audience name & defaults → Audience ID
MAILCHIMP_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxx-us21
MAILCHIMP_SERVER_PREFIX=us21
MAILCHIMP_AUDIENCE_ID=xxxxxxxxxxxx

# ── Internal notifications ──────────────────────────────────────────
# Who gets an email for every form submission (also add extras in email.config.ts notify array)
INTERNAL_NOTIFY_EMAIL=owner@clientsite.com
