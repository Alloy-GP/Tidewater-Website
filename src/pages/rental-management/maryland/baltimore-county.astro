---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Topbar from '../../../components/Topbar.astro';
import Header from '../../../components/Header.astro';
import Breadcrumb from '../../../components/Breadcrumb.astro';
import Footer from '../../../components/Footer.astro';
import CTA from '../../../components/CTA.astro';
import RentalGeo, { GEO_DATA_BALTIMORE_COUNTY as geo } from '../../../components/RentalGeo.jsx';
import { breadcrumbSchema, serviceSchema, faqSchema, localBusinessSchema } from '../../../lib/schema';
import { SITE } from '../../../config/site';

const pageUrl = `${SITE.url}/rental-management/maryland/baltimore-county`;

const schemas = [
  serviceSchema({
    name: 'Rental Property Management — Baltimore County, MD',
    description: geo.schemaDesc,
    url: pageUrl,
    areaServed: 'Baltimore County, Maryland',
  }),
  breadcrumbSchema([
    { name: 'Home', url: SITE.url + '/' },
    { name: 'Rental Management', url: SITE.url + '/rental-management' },
    { name: 'Maryland', url: SITE.url + '/rental-management/maryland' },
    { name: 'Baltimore County', url: pageUrl },
  ]),
  faqSchema(geo.faqs),
  localBusinessSchema({
    office: 'hq',
    url: pageUrl,
    areaServed: { name: 'Baltimore County', type: 'AdministrativeArea', containedIn: 'Maryland' },
    description: geo.schemaDesc,
  }),
];
---

<BaseLayout
  title="Baltimore County Property Management | Flat 8.5% Fee | Tidewater"
  description="Baltimore County property management from Tidewater's Owings Mills headquarters — Towson, Owings Mills, Pikesville, Cockeysville, Catonsville. Flat 8.5% fee, no tenant-placement upcharge, county rental license handled."
  ogType="website"
  pageSchema={schemas}
  pageStylesheet="styles-geo.css"
>
  <Topbar />
  <Header activeNav="rental" logoSrc="/assets/Tidewater-Management-Logo.svg" logoAlt="Tidewater Management" />
  <Breadcrumb variant="management" crumbs={[
    { label: 'Rental Management', href: '/rental-management' },
    { label: 'Maryland', href: '/rental-management/maryland' },
    { label: 'Baltimore County' },
  ]} />
  <RentalGeo geo={geo} client:load />
  <CTA />
  <Footer />
</BaseLayout>
